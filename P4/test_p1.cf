struct Point {
    int x;
    int y;
};
int f(int x, bool b) {
}
void g() {
    int a;
    bool b;
    struct Point p;
    p.x = a;
    b = a == 3;
    f(a + p.y*2, b);
    g();
}

int x;
int b;

struct Node {
   struct Point pt;
};
bool z;
int createNode(int a) {
    struct Node n;
    int x;
    int y;
    int pt;
    z = 1;
    //n.pt = 1;
    n.pt.x = 1;
    z=(n.pt.y+1);
    n.pt.x = a;
    n.pt = b;
    if (b == -1) { 
        x = 4+3*5-y; 
        while (true) { 
            y = y*2+x; 
        } 
    } 
    else {
        x = 0;
    }
}

int m(int x, bool b) {
    return 5;
}

